[project]
name = "llm-knowledge-harvester"
version = "0.1.0"
description = "A personalized AI agent system that curates, summarizes, and ranks articles based on your interests."
readme = "README.md"
requires-python = ">=3.12"
dependencies = []
